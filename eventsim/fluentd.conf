<source>
  @type tail
  @id auth_events
  <parse>
    @type json
  </parse>
  path /fluentd/etc/generated/*/auth_events
  pos_file /fluentd/etc/pos/auth_events.pos
  tag auth
  read_from_head true
</source>

<match auth>
   @type stdout
</match>
